from context import precis

import json

import unittest


class TestUtil(unittest.TestCase):
    """Test the `util` module functionality.
    """

## 4 test cases: one to test basic loading, one with override, one with poorly formatted override, and another with a bad file location (last 2 to check errors)
